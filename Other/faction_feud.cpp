#include <bits/stdc++.h>

using namespace std;

#define long long long int

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int Q; cin >> Q;
    while (Q--) {
        long N; cin >> N;

        long ans = N / 2LL;
        cout << ans << "\n";
    }
}